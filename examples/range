(rule (range ?low ?high ?step ())
  (clojure-predicate ">" ?low ?high))

(rule (range ?low ?high ?step (?low . ?rest))
  (and
    (clojure-predicate "<=" ?low ?high)
    (is ?new-low (+ ?low ?step))
    (range ?new-low ?high ?step ?rest)))
